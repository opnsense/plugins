<form>
    <field>
        <id>caddy.general.DynDnsIpVersions</id>
        <label>DynDns IP Version</label>
        <type>dropdown</type>
        <help><![CDATA[<strong>Select the DynDns IP Version:</strong> <code>IPv4+IPv6</code> to set IPv4 A-Records and IPv6 AAAA-Records, <code>IPv4 only</code> for only A-Records, <code>IPv6 only</code> for only AAAA-Records.]]></help>
    </field>
    <field>
        <id>caddy.general.DynDnsCheckInterval</id>
        <label>DynDns Check Interval</label>
        <type>text</type>
        <help><![CDATA[<strong>Set the interval to poll for changes in the IP address.</strong> The default is 5 minutes. Values can range from 1 to 1440 minutes.]]></help>
    </field>
    <field>
        <id>caddy.general.DynDnsTTL</id>
        <label>DynDns TTL</label>
        <type>text</type>
        <help><![CDATA[<strong>Set the TTL (Time to Live) for DNS records.</strong> The default is 1 hour. Values can range from 1 to 24 hours.]]></help>
    </field>
    <field>
        <type>header</type>
        <label>Additional Checks</label>
        <collapse>true</collapse>
    </field>
    <field>
        <id>caddy.general.DynDnsSimpleHttp</id>
        <label>DynDns Check Http</label>
        <type>text</type>
        <help><![CDATA[<strong>Enter a URL to test the current IP address of the firewall via the HTTP protocol.</strong> <em>This is generally not needed</em> as Caddy uses default providers to test the current IP addresses. If a custom provider is preferred, enter the <code>https://</code> link to an IP address testing website.]]></help>
    </field>
    <field>
        <id>caddy.general.DynDnsInterface</id>
        <label>DynDns Check Interface</label>
        <type>dropdown</type>
        <help><![CDATA[<strong>Select an interface to extract the current IP addresses of the firewall</strong>. <em>This is generally not needed</em> as Caddy uses default providers to test the current IP addresses. Depending on the specified DynDns IP Version, at most one IPv6 Global Unicast Address and one IPv4 non-RFC1918 Address will be extracted.]]></help>
    </field>
</form>

<form>
    <field>
        <id>header.HeaderUpDown</id>
        <label>Header</label>
        <type>dropdown</type>
        <help><![CDATA[<strong>header_up</strong> sets, adds (with the <code>+</code> prefix), deletes (with the <code>-</code> prefix), or performs a replacement (by using two arguments, a search and replacement) in a request header going upstream to the backend. <strong>header_down</strong> sets, adds (with the <code>+</code> prefix), deletes (with the <code>-</code> prefix), or performs a replacement (by using two arguments, a search and replacement) in a response header coming downstream from the backend. For more information: <a href="https://caddyserver.com/docs/caddyfile/directives/reverse_proxy#headers">Caddy Documentation on Headers</a>.]]></help>
    </field>
    <field>
        <id>header.HeaderType</id>
        <label>Header Type</label>
        <hint>Host</hint>
        <type>text</type>
        <help><![CDATA[<strong>Enter a header,</strong> for example <code>Host</code>. Use the <code>+</code> or <code>-</code> prefix to add or remove this header, for example <code>-Host</code> or <code>+Host</code>. A suffix match like <code>-Host-*</code> is also supported. To replace a header, use <code>Host</code> without <code>+</code> or <code>-</code>.]]></help>
    </field>
    <field>
        <id>header.HeaderValue</id>
        <label>Header Value</label>
        <hint>{upstream_hostport}</hint>
        <type>text</type>
        <help><![CDATA[<strong>Enter a value for the selected header.</strong> One of the most common options is <code>{upstream_hostport}</code>. It's also possible to use a regular expression to search for a specific value in a header. For example: <code>^prefix-([A-Za-z0-9]*)$</code> which uses the regular expression language RE2 included in Go.]]></help>
    </field>
    <field>
        <id>header.HeaderReplace</id>
        <label>Header Replace</label>
        <type>text</type>
        <help><![CDATA[If a regular expression is used to search for a Header Value, the replacement string can be set here. For example: <code>replaced-$1-suffix</code> which expands the replacement string, allowing the use of captured values, <code>$1</code> being the first capture group.]]></help>
    </field>
    <field>
        <id>header.description</id>
        <label>Description</label>
        <type>text</type>
        <help><![CDATA[<strong>Enter a description for this header.</strong>]]></help>
    </field>
</form>
